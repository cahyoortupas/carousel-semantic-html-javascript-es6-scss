{
  "name": "carousel-javascript-scss",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build:scss": "sass src/scss/styles.scss dist/main.css --no-source-map",
    "build:js": "esbuild src/js/app.js --bundle --outfile=dist/bundle.js",
    "build": "npm run build:scss && npm run build:js",
    "dev": "npm run build && npm run serve"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "esbuild": "^0.24.0",
    "sass": "^1.79.3"
  }
}
